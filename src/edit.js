// TODO: Edit functionality requires a read-capable .docx library.
// The docx library (v9+) only supports document creation, not editing.
// Future: integrate jszip + xmldom or docxtemplater for edit operations.
// Planned API:
//   --input/--output (same file for in-place, different for copy)
//   --find/--replace (text replacement)
//   --append (add paragraph)
//   --replace-index N --text (replace paragraph N)

